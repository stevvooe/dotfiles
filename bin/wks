#!/usr/bin/env bash

set -euo pipefail

emsg() {
  printf '%s\n' "$*" >&2
}

usage() {
  cat <<'EOF'
Usage:
  wks                  # interactive switch (fzf)
  wks ls [--status]
  wks new <name> [--base <ref>]
  wks new --fix <name> [--base <ref>]
  wks new --feat <name> [--base <ref>]
  wks new <name> --branch <branch> [--base <ref>]
  wks names
  wks rows
  wks diff [name]
  wks sw [name]
  wks switch [name]
  wks tmux [name]
  wks tmux --new <name> [--base <ref>]
  wks tmux --new --fix <name> [--base <ref>]
  wks tmux --new --feat <name> [--base <ref>]
  wks tmux --new <name> --branch <branch> [--base <ref>]
  wks oc [name] [-- <opencode-args...>]
  wks oc --new <name> [--base <ref>] [-- <opencode-args...>]
  wks oc --new --fix <name> [--base <ref>] [-- <opencode-args...>]
  wks oc --new --feat <name> [--base <ref>] [-- <opencode-args...>]
  wks oc --new <name> --branch <branch> [--base <ref>] [-- <opencode-args...>]
  wks rm <name> [--force] [--prune-branch]
  wks path [name]

Commands:
  ls [--status]        List worktrees (--status adds dirty/clean check)
  new                  Create .workspaces/<name> from --base (default: origin/HEAD)
  names                Print workspace names (for scripting/completion)
  rows                 Print tab-delimited workspace rows (name, branch, state, path)
  diff                 Show git status and diff for a workspace
  sw, switch           Print selected workspace path (fzf when no name)
  tmux                 Launch devtmux in a workspace; use --new to create first
  oc                   Launch opencode in a workspace; use --new to create first
  rm                   Remove workspace worktree (refuses dirty unless --force)
                       --prune-branch deletes the local branch after removal
  path                 Print current workspace root, or named workspace path

Notes:
  - Workspaces live under <main checkout>/.workspaces
  - New workspaces default to branch wks/<name>
  - --fix/--feat create fix/<user>/<name> or feat/<user>/<name>
  - --branch uses an explicit branch name
  - wks oc defaults to edit and todowrite permission allow unless OPENCODE_PERMISSION is already set
  - Use a shell wrapper to cd after switch output
EOF
}

require_git_repo() {
  if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
    emsg "error: not inside a git repository"
    exit 1
  fi
}

init_repo_paths() {
  git_common_dir=$(git rev-parse --path-format=absolute --git-common-dir)
  main_root=$(cd "$git_common_dir/.." && pwd -P)
  workspaces_dir="$main_root/.workspaces"
  current_root=$(git rev-parse --show-toplevel)
}

ensure_local_exclude() {
  local exclude_file

  exclude_file=$(git rev-parse --path-format=absolute --git-path info/exclude)
  if [ ! -f "$exclude_file" ]; then
    touch "$exclude_file"
  fi

  if ! grep -qxF '/.workspaces/' "$exclude_file"; then
    printf '\n/.workspaces/\n' >>"$exclude_file"
  fi
}

branch_for_path() {
  local path=$1
  local branch

  if branch=$(git -C "$path" symbolic-ref --short -q HEAD 2>/dev/null); then
    printf '%s\n' "$branch"
    return
  fi

  branch=$(git -C "$path" rev-parse --short HEAD)
  printf 'detached@%s\n' "$branch"
}

state_for_path() {
  local path=$1

  if [ -n "$(git -C "$path" status --porcelain --ignore-submodules=dirty)" ]; then
    printf 'dirty\n'
  else
    printf 'clean\n'
  fi
}

workspace_rows() {
  local with_state=0
  if [ "${1:-}" = "--with-state" ]; then
    with_state=1
  fi

  local -a ws_paths ws_names
  ws_paths=("$main_root")
  ws_names=("main")

  if [ -d "$workspaces_dir" ]; then
    for path in "$workspaces_dir"/*; do
      [ -d "$path" ] || continue
      git -C "$path" rev-parse --is-inside-work-tree >/dev/null 2>&1 || continue
      ws_paths+=("$path")
      ws_names+=("$(basename "$path")")
    done
  fi

  local tmpdir=
  if [ "$with_state" -eq 1 ]; then
    tmpdir=$(mktemp -d)
    for i in "${!ws_paths[@]}"; do
      ( state_for_path "${ws_paths[$i]}" >"$tmpdir/$i" ) &
    done
    wait
  fi

  local i branch state
  for i in "${!ws_paths[@]}"; do
    branch=$(branch_for_path "${ws_paths[$i]}")
    if [ "$with_state" -eq 1 ]; then
      state=$(<"$tmpdir/$i")
    else
      state="-"
    fi
    printf '%s\t%s\t%s\t%s\n' "${ws_names[$i]}" "$branch" "$state" "${ws_paths[$i]}"
  done

  if [ -n "$tmpdir" ]; then
    rm -rf "$tmpdir"
  fi
}

workspace_names() {
  local name branch state path

  workspace_rows | while IFS=$'\t' read -r name branch state path; do
    printf '%s\n' "$name"
  done
}

resolve_workspace_path() {
  local name=$1
  local path

  if [ "$name" = "main" ]; then
    printf '%s\n' "$main_root"
    return 0
  fi

  path="$workspaces_dir/$name"
  if [ -d "$path" ] && git -C "$path" rev-parse --is-inside-work-tree >/dev/null 2>&1; then
    printf '%s\n' "$path"
    return 0
  fi

  emsg "error: workspace '$name' not found"
  return 1
}

validate_workspace_name() {
  local name=$1

  if [ -z "$name" ]; then
    emsg "error: workspace name cannot be empty"
    exit 1
  fi

  if [ "$name" = "main" ]; then
    emsg "error: 'main' is reserved"
    exit 1
  fi

  if [[ ! "$name" =~ ^[A-Za-z0-9._-]+$ ]]; then
    emsg "error: workspace name must match [A-Za-z0-9._-]+"
    exit 1
  fi
}

branch_user() {
  local user=${WKS_BRANCH_USER:-${GITHUB_USER:-${USER:-}}}

  if [ -z "$user" ]; then
    emsg "error: could not determine branch user (set WKS_BRANCH_USER)"
    exit 1
  fi

  printf '%s\n' "$user"
}

validate_branch_name() {
  local branch=$1

  if ! git check-ref-format --branch "$branch" >/dev/null 2>&1; then
    emsg "error: invalid branch name '$branch'"
    exit 1
  fi
}

branch_for_kind() {
  local kind=$1
  local name=$2

  printf '%s/%s/%s\n' "$kind" "$(branch_user)" "$name"
}

resolve_workspace_branch() {
  local name=$1
  local explicit_branch=$2
  local branch_kind=$3

  if [ -n "$explicit_branch" ] && [ -n "$branch_kind" ]; then
    emsg "error: --branch cannot be combined with --$branch_kind"
    exit 1
  fi

  if [ -n "$explicit_branch" ]; then
    printf '%s\n' "$explicit_branch"
    return 0
  fi

  if [ -n "$branch_kind" ]; then
    branch_for_kind "$branch_kind" "$name"
    return 0
  fi

  printf 'wks/%s\n' "$name"
}

default_workspace_base() {
  local branch base

  if command -v gh >/dev/null 2>&1; then
    if branch=$(gh repo view --json defaultBranchRef --jq .defaultBranchRef.name 2>/dev/null) && [ -n "$branch" ]; then
      printf 'origin/%s\n' "$branch"
      return 0
    fi
  fi

  if base=$(git symbolic-ref --quiet --short refs/remotes/origin/HEAD 2>/dev/null); then
    printf '%s\n' "$base"
    return 0
  fi

  if git show-ref --verify --quiet refs/remotes/origin/main; then
    printf 'origin/main\n'
    return 0
  fi

  if git show-ref --verify --quiet refs/remotes/origin/master; then
    printf 'origin/master\n'
    return 0
    fi

  emsg "error: could not determine default remote base (expected origin/HEAD, origin/main, or origin/master)"
  exit 1
}

set_branch_kind() {
  local current_kind=$1
  local next_kind=$2

  if [ -n "$current_kind" ] && [ "$current_kind" != "$next_kind" ]; then
    emsg "error: --$current_kind cannot be combined with --$next_kind"
    exit 1
  fi

  printf '%s\n' "$next_kind"
}

consume_option_value() {
  local flag=$1
  local label=$2
  shift 2

  if [ $# -eq 0 ] || [[ "$1" == -* ]]; then
    emsg "error: $flag requires a $label"
    exit 1
  fi

  printf '%s\n' "$1"
}

devtmux_cmd() {
  local cmd

  cmd=$(command -v devtmux 2>/dev/null || true)
  if [ -z "$cmd" ] && [ -x "$main_root/bin/devtmux" ]; then
    cmd=$main_root/bin/devtmux
  fi

  if [ -z "$cmd" ]; then
    emsg "error: devtmux not found in PATH"
    exit 1
  fi

  printf '%s\n' "$cmd"
}

launch_devtmux() {
  local path=$1
  local cmd name

  cmd=$(devtmux_cmd)
  if [ "$path" = "$main_root" ]; then
    cd "$path"
    exec env WKS_ROOT="$path" "$cmd"
  fi

  name=$(basename "$path")
  cd "$path"
  exec env WKS_NAME="$name" WKS_ROOT="$path" "$cmd"
}

cmd_ls() {
  local name branch state path marker with_state_flag=""

  while [ $# -gt 0 ]; do
    case "$1" in
      --status) with_state_flag="--with-state"; shift ;;
      *) emsg "error: unknown option '$1'"; exit 1 ;;
    esac
  done

  workspace_rows $with_state_flag | while IFS=$'\t' read -r name branch state path; do
    marker=' '
    if [ "$path" = "$current_root" ]; then
      marker='*'
    fi
    if [ -n "$with_state_flag" ]; then
      printf '%s %-16s %-24s %-6s %s\n' "$marker" "$name" "$branch" "$state" "$path"
    else
      printf '%s %-16s %-24s %s\n' "$marker" "$name" "$branch" "$path"
    fi
  done
}

cmd_names() {
  workspace_names
}

cmd_rows() {
  local with_state_flag=""

  while [ $# -gt 0 ]; do
    case "$1" in
      --status) with_state_flag="--with-state"; shift ;;
      *) emsg "error: unknown option '$1'"; exit 1 ;;
    esac
  done

  workspace_rows $with_state_flag
}

cmd_diff() {
  local name=${1:-} path status_output unstaged_diff staged_diff

  if [ $# -gt 1 ]; then
    emsg "error: unexpected extra argument '$2'"
    exit 1
  fi

  if [ -n "$name" ]; then
    path=$(resolve_workspace_path "$name")
  else
    path=$current_root
  fi

  status_output=$(git -C "$path" status --short --branch)
  unstaged_diff=$(git -C "$path" diff)
  staged_diff=$(git -C "$path" diff --cached)

  printf 'Workspace: %s\n' "$path"
  printf '\n== Status ==\n'
  if [ -n "$status_output" ]; then
    printf '%s\n' "$status_output"
  else
    printf 'working tree clean\n'
  fi

  printf '\n== Unstaged Diff ==\n'
  if [ -n "$unstaged_diff" ]; then
    printf '%s\n' "$unstaged_diff"
  else
    printf 'No unstaged changes.\n'
  fi

  printf '\n== Staged Diff ==\n'
  if [ -n "$staged_diff" ]; then
    printf '%s\n' "$staged_diff"
  else
    printf 'No staged changes.\n'
  fi
}

cmd_switch() {
  local name=${1:-}

  if [ -n "$name" ]; then
    resolve_workspace_path "$name"
    return 0
  fi

  pick_workspace_path
}

pick_workspace_path() {
  local selected path

  if ! command -v fzf >/dev/null 2>&1; then
    emsg "error: fzf is required for interactive switching"
    exit 1
  fi

  selected=$(workspace_rows | fzf --prompt='workspace> ' --height=50% --layout=reverse --delimiter=$'\t' --with-nth=1,2)
  if [ -z "$selected" ]; then
    exit 1
  fi

  path=${selected##*$'\t'}
  printf '%s\n' "$path"
}

cmd_oc() {
  local name path create_new base branch branch_kind explicit_branch base_explicit
  local -a opencode_args

  if ! command -v opencode >/dev/null 2>&1; then
    emsg "error: opencode not found in PATH"
    exit 1
  fi

  name=
  create_new=0
  base=
  branch=
  branch_kind=
  explicit_branch=
  base_explicit=0
  opencode_args=()

  while [ $# -gt 0 ]; do
    case "$1" in
      --new)
        create_new=1
        shift
        if [ $# -gt 0 ] && [[ "$1" != -* ]]; then
          name=$1
          shift
        fi
        ;;
      --new=*)
        create_new=1
        name=${1#--new=}
        shift
        ;;
      --base)
        shift
        if [ $# -eq 0 ]; then
          emsg "error: --base requires a ref"
          exit 1
        fi
        base=$1
        base_explicit=1
        shift
        ;;
      --branch)
        shift
        explicit_branch=$(consume_option_value --branch "branch name" "$@")
        shift
        ;;
      --branch=*)
        explicit_branch=${1#--branch=}
        if [ -z "$explicit_branch" ]; then
          emsg "error: --branch requires a branch name"
          exit 1
        fi
        shift
        ;;
      --fix)
        shift
        branch_kind=$(set_branch_kind "$branch_kind" fix)
        if [ -n "$name" ]; then
          continue
        fi
        name=$(consume_option_value --fix "workspace name" "$@")
        shift
        ;;
      --fix=*)
        if [ -n "$name" ]; then
          emsg "error: workspace name already set to '$name'"
          exit 1
        fi
        branch_kind=$(set_branch_kind "$branch_kind" fix)
        name=${1#--fix=}
        shift
        ;;
      --feat)
        shift
        branch_kind=$(set_branch_kind "$branch_kind" feat)
        if [ -n "$name" ]; then
          continue
        fi
        name=$(consume_option_value --feat "workspace name" "$@")
        shift
        ;;
      --feat=*)
        if [ -n "$name" ]; then
          emsg "error: workspace name already set to '$name'"
          exit 1
        fi
        branch_kind=$(set_branch_kind "$branch_kind" feat)
        name=${1#--feat=}
        shift
        ;;
      --)
        shift
        opencode_args+=("$@")
        break
        ;;
      -* )
        opencode_args+=("$1")
        shift
        ;;
      *)
        if [ -z "$name" ]; then
          name=$1
        else
          opencode_args+=("$1")
        fi
        shift
        ;;
    esac
  done

  if [ "$create_new" -eq 1 ]; then
    if [ -z "$name" ]; then
      emsg "error: --new requires a workspace name"
      exit 1
    fi
    if [ "$base_explicit" -eq 0 ]; then
      base=$(default_workspace_base)
    fi
    branch=$(resolve_workspace_branch "$name" "$explicit_branch" "$branch_kind")
    path=$(cmd_new "$name" --base "$base" --branch "$branch")
  else
    if [ "$base_explicit" -eq 1 ]; then
      emsg "error: --base can only be used with --new"
      exit 1
    fi
    if [ -n "$explicit_branch" ] || [ -n "$branch_kind" ]; then
      emsg "error: --branch, --fix, and --feat can only be used with --new"
      exit 1
    fi
    if [ -n "$name" ]; then
      path=$(resolve_workspace_path "$name")
    else
      path=$(pick_workspace_path)
    fi
  fi

  set -- ${opencode_args[@]+"${opencode_args[@]}"}

  if [ -z "${OPENCODE_PERMISSION:-}" ]; then
    exec env OPENCODE_PERMISSION='{"edit":"allow","todowrite":"allow"}' opencode "$path" "$@"
  fi

  exec opencode "$path" "$@"
}

cmd_tmux() {
  local name path create_new base branch branch_kind explicit_branch base_explicit

  name=
  create_new=0
  base=
  branch=
  branch_kind=
  explicit_branch=
  base_explicit=0

  while [ $# -gt 0 ]; do
    case "$1" in
      --new)
        create_new=1
        shift
        if [ $# -gt 0 ] && [[ "$1" != -* ]]; then
          name=$1
          shift
        fi
        ;;
      --new=*)
        create_new=1
        name=${1#--new=}
        shift
        ;;
      --base)
        shift
        if [ $# -eq 0 ]; then
          emsg "error: --base requires a ref"
          exit 1
        fi
        base=$1
        base_explicit=1
        shift
        ;;
      --branch)
        shift
        explicit_branch=$(consume_option_value --branch "branch name" "$@")
        shift
        ;;
      --branch=*)
        explicit_branch=${1#--branch=}
        if [ -z "$explicit_branch" ]; then
          emsg "error: --branch requires a branch name"
          exit 1
        fi
        shift
        ;;
      --fix)
        shift
        branch_kind=$(set_branch_kind "$branch_kind" fix)
        if [ -n "$name" ]; then
          continue
        fi
        name=$(consume_option_value --fix "workspace name" "$@")
        shift
        ;;
      --fix=*)
        if [ -n "$name" ]; then
          emsg "error: workspace name already set to '$name'"
          exit 1
        fi
        branch_kind=$(set_branch_kind "$branch_kind" fix)
        name=${1#--fix=}
        shift
        ;;
      --feat)
        shift
        branch_kind=$(set_branch_kind "$branch_kind" feat)
        if [ -n "$name" ]; then
          continue
        fi
        name=$(consume_option_value --feat "workspace name" "$@")
        shift
        ;;
      --feat=*)
        if [ -n "$name" ]; then
          emsg "error: workspace name already set to '$name'"
          exit 1
        fi
        branch_kind=$(set_branch_kind "$branch_kind" feat)
        name=${1#--feat=}
        shift
        ;;
      -h|--help)
        usage
        exit 0
        ;;
      -* )
        emsg "error: unknown option '$1'"
        exit 1
        ;;
      *)
        if [ -n "$name" ]; then
          emsg "error: unexpected extra argument '$1'"
          exit 1
        fi
        name=$1
        shift
        ;;
    esac
  done

  if [ "$create_new" -eq 1 ]; then
    if [ -z "$name" ]; then
      emsg "error: --new requires a workspace name"
      exit 1
    fi
    if [ "$base_explicit" -eq 0 ]; then
      base=$(default_workspace_base)
    fi
    branch=$(resolve_workspace_branch "$name" "$explicit_branch" "$branch_kind")
    path=$(cmd_new "$name" --base "$base" --branch "$branch")
  else
    if [ "$base_explicit" -eq 1 ]; then
      emsg "error: --base can only be used with --new"
      exit 1
    fi
    if [ -n "$explicit_branch" ] || [ -n "$branch_kind" ]; then
      emsg "error: --branch, --fix, and --feat can only be used with --new"
      exit 1
    fi
    if [ -n "$name" ]; then
      path=$(resolve_workspace_path "$name")
    else
      path=$(pick_workspace_path)
    fi
  fi

  launch_devtmux "$path"
}

cmd_new() {
  local name base branch target branch_kind explicit_branch

  if [ $# -lt 1 ]; then
    emsg "error: missing workspace name"
    usage
    exit 1
  fi

  name=
  base=
  branch_kind=
  explicit_branch=

  while [ $# -gt 0 ]; do
    case "$1" in
      --base)
        shift
        if [ $# -eq 0 ]; then
          emsg "error: --base requires a ref"
          exit 1
        fi
        base=$1
        shift
        ;;
      --branch)
        shift
        explicit_branch=$(consume_option_value --branch "branch name" "$@")
        shift
        ;;
      --branch=*)
        explicit_branch=${1#--branch=}
        if [ -z "$explicit_branch" ]; then
          emsg "error: --branch requires a branch name"
          exit 1
        fi
        shift
        ;;
      --fix)
        branch_kind=$(set_branch_kind "$branch_kind" fix)
        shift
        ;;
      --fix=*)
        if [ -n "$name" ]; then
          emsg "error: workspace name already set to '$name'"
          exit 1
        fi
        branch_kind=$(set_branch_kind "$branch_kind" fix)
        name=${1#--fix=}
        shift
        ;;
      --feat)
        branch_kind=$(set_branch_kind "$branch_kind" feat)
        shift
        ;;
      --feat=*)
        if [ -n "$name" ]; then
          emsg "error: workspace name already set to '$name'"
          exit 1
        fi
        branch_kind=$(set_branch_kind "$branch_kind" feat)
        name=${1#--feat=}
        shift
        ;;
      -h|--help)
        usage
        exit 0
        ;;
      *)
        if [ -z "$name" ]; then
          name=$1
          shift
          continue
        fi
        emsg "error: unknown option '$1'"
        exit 1
        ;;
    esac
  done

  if [ -z "$name" ]; then
    emsg "error: missing workspace name"
    exit 1
  fi

  if [ -z "$base" ]; then
    base=$(default_workspace_base)
  fi

  validate_workspace_name "$name"
  branch=$(resolve_workspace_branch "$name" "$explicit_branch" "$branch_kind")
  validate_branch_name "$branch"
  target="$workspaces_dir/$name"

  if [ -e "$target" ]; then
    emsg "error: workspace path already exists: $target"
    exit 1
  fi

  if git show-ref --verify --quiet "refs/heads/$branch"; then
    emsg "error: branch already exists: $branch"
    exit 1
  fi

  ensure_local_exclude
  mkdir -p "$workspaces_dir"
  git worktree add -b "$branch" "$target" "$base" >/dev/null
  printf '%s\n' "$target"
}

cmd_rm() {
  local name force prune_branch path branch

  name=
  force=0
  prune_branch=0

  while [ $# -gt 0 ]; do
    case "$1" in
      -f|--force)
        force=1
        shift
        ;;
      --prune-branch)
        prune_branch=1
        shift
        ;;
      -*)
        emsg "error: unknown option '$1'"
        exit 1
        ;;
      *)
        if [ -n "$name" ]; then
          emsg "error: unexpected extra argument '$1'"
          exit 1
        fi
        name=$1
        shift
        ;;
    esac
  done

  if [ -z "$name" ]; then
    emsg "error: missing workspace name"
    usage
    exit 1
  fi

  path=$(resolve_workspace_path "$name")

  if [ "$path" = "$main_root" ]; then
    emsg "error: cannot remove main workspace"
    exit 1
  fi

  if [ $force -eq 0 ] && [ -n "$(git -C "$path" status --porcelain --ignore-submodules=dirty)" ]; then
    emsg "error: workspace has uncommitted changes (use --force to remove)"
    exit 1
  fi

  branch=$(branch_for_path "$path")

  if [ $force -eq 1 ]; then
    git worktree remove --force "$path"
  else
    git worktree remove "$path"
  fi

  git worktree prune >/dev/null 2>&1 || true

  if [ "$prune_branch" -eq 1 ] && [ -n "$branch" ] && [[ "$branch" != detached@* ]]; then
    if git show-ref --verify --quiet "refs/heads/$branch"; then
      git branch -D "$branch"
    fi
  fi
}

cmd_path() {
  local name=${1:-}

  if [ -z "$name" ]; then
    printf '%s\n' "$current_root"
    return
  fi

  resolve_workspace_path "$name"
}

main() {
  local cmd

  require_git_repo
  init_repo_paths

  cmd=${1:-switch}
  if [ $# -gt 0 ]; then
    shift
  fi

  case "$cmd" in
    -h|--help|help)
      usage
      ;;
    ls)
      cmd_ls "$@"
      ;;
    new)
      cmd_new "$@"
      ;;
    names)
      cmd_names "$@"
      ;;
    rows)
      cmd_rows "$@"
      ;;
    diff)
      cmd_diff "$@"
      ;;
    sw|switch)
      cmd_switch "$@"
      ;;
     oc)
      cmd_oc "$@"
      ;;
    tmux)
      cmd_tmux "$@"
      ;;
    rm)
      cmd_rm "$@"
      ;;
    path)
      cmd_path "$@"
      ;;
    *)
      emsg "error: unknown command '$cmd'"
      usage
      exit 1
      ;;
  esac
}

main "$@"
